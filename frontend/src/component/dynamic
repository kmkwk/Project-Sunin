import dynamic from 'next/dynamic';

const Alarm = dynamic(
  () => import('../component/Alarm'),
  { ssr: false }
)

function writePost():JSX.Element {
  return (
    <>
      <Alarm />
    </>
  )
}

export default writePost;